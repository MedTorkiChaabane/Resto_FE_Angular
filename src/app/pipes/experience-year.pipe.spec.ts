import { ExperienceYearPipe } from './experience-year.pipe';

describe('ExperienceYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceYearPipe();
    expect(pipe).toBeTruthy();
  });
});
